## C# Coding Style

- Prefer explicit types over `var` for readability.
- Only use `var` when the explicit type is excessively verbose and the inferred type is obvious from the right-hand side.
- Follow repository `.editorconfig` rules; do not introduce new style drift.
