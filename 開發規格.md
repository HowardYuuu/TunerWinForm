# 即時聊天室 Web 應用程式 - 開發規格

## 專案概述

### 專案名稱
ChatRoom - 即時聊天室

### 專案描述
開發一個基於 Web 的即時聊天室應用程式，允許多個用戶同時在線交流。使用 ASP.NET Core 8.0 和 SignalR 技術實現即時雙向通訊。

### 技術堆疊
- **後端框架**: ASP.NET Core 8.0
- **即時通訊**: SignalR
- **前端**: HTML5, CSS3, JavaScript (Vanilla JS)
- **開發語言**: C# 12.0

---

## 功能需求

### 1. 用戶連線管理

#### 1.1 連線功能
- **需求描述**: 用戶可以輸入暱稱並連線進入聊天室
- **實作細節**:
  - 提供暱稱輸入框
  - 連線按鈕，點擊後建立 SignalR 連線
  - 驗證暱稱不可為空白
  - 成功連線後顯示歡迎訊息
  
#### 1.2 即時建立連線事件
- **需求描述**: 當用戶連線時，通知所有在線用戶
- **實作細節**:
  - 新用戶加入時，廣播通知給所有已連線用戶
  - 顯示系統訊息：「[用戶名稱] 加入了聊天室」
  - 更新在線用戶列表

#### 1.3 顯示上線狀態
- **需求描述**: 顯示目前所有在線用戶列表
- **實作細節**:
  - 維護在線用戶清單
  - 即時更新用戶列表（新增/移除）
  - 顯示在線人數統計
  - 用戶列表包含：暱稱、連線時間

#### 1.4 關閉連線
- **需求描述**: 用戶可以主動關閉連線或離開聊天室
- **實作細節**:
  - 提供「離開聊天室」按鈕
  - 關閉瀏覽器或分頁時自動斷線
  - 斷線時通知所有在線用戶
  - 顯示系統訊息：「[用戶名稱] 離開了聊天室」
  - 從在線用戶列表中移除

### 2. 訊息傳送功能

#### 2.1 發送訊息
- **需求描述**: 用戶可以發送文字訊息給所有在線用戶
- **實作細節**:
  - 訊息輸入框
  - 發送按鈕
  - 支援 Enter 鍵快速發送
  - 訊息不可為空白
  - 發送後清空輸入框

#### 2.2 接收訊息
- **需求描述**: 即時接收其他用戶發送的訊息
- **實作細節**:
  - 訊息顯示區域
  - 顯示發送者暱稱
  - 顯示發送時間
  - 自動捲動到最新訊息
  - 區分自己和他人的訊息（不同顏色或樣式）

---

## 系統架構

### 後端架構

#### SignalR Hub
```
ChatHub
├── OnConnectedAsync()      // 處理用戶連線
├── OnDisconnectedAsync()   // 處理用戶斷線
├── SendMessage()           // 處理訊息發送
└── GetOnlineUsers()        // 取得在線用戶列表
```

#### 資料模型
```csharp
// 用戶資訊
public class UserInfo
{
    public string ConnectionId { get; set; }
    public string Nickname { get; set; }
    public DateTime ConnectedAt { get; set; }
}

// 訊息
public class ChatMessage
{
    public string Sender { get; set; }
    public string Content { get; set; }
    public DateTime Timestamp { get; set; }
}
```

### 前端架構

#### 頁面結構
```
index.html
├── 標題區
├── 連線區域
│   ├── 暱稱輸入框
│   └── 連線/離開按鈕
├── 聊天區域
│   ├── 訊息顯示區
│   └── 訊息輸入區
└── 側邊欄
    ├── 在線人數統計
    └── 在線用戶列表
```

---

## 介面設計規範

### 視覺設計
- **配色方案**:
  - 主色調: #4A90E2 (藍色)
  - 輔色: #50E3C2 (青綠色)
  - 背景色: #F5F7FA (淺灰)
  - 文字色: #333333 (深灰)

- **版面配置**:
  - 響應式設計，支援桌面和移動設備
  - 主要區域: 70% 聊天區 + 30% 用戶列表
  - 固定頂部標題欄
  - 固定底部輸入欄

### 互動設計
- 連線成功後隱藏連線區域
- 訊息輸入框獲得焦點
- 發送訊息時有動畫效果
- 新訊息淡入效果
- 系統訊息與用戶訊息有不同樣式

---

## API 規格

### SignalR Hub 方法

#### 客戶端調用方法 (Client -> Server)

1. **JoinChat**
   - 參數: `string nickname`
   - 說明: 用戶加入聊天室
   - 回傳: `Task`

2. **SendMessage**
   - 參數: `string message`
   - 說明: 發送訊息
   - 回傳: `Task`

3. **LeaveChat**
   - 參數: 無
   - 說明: 用戶離開聊天室
   - 回傳: `Task`

#### 服務端推送方法 (Server -> Client)

1. **ReceiveMessage**
   - 參數: `string sender, string message, DateTime timestamp`
   - 說明: 接收訊息

2. **UserJoined**
   - 參數: `string nickname, int onlineCount`
   - 說明: 用戶加入通知

3. **UserLeft**
   - 參數: `string nickname, int onlineCount`
   - 說明: 用戶離開通知

4. **UpdateOnlineUsers**
   - 參數: `List<UserInfo> users`
   - 說明: 更新在線用戶列表

5. **SystemMessage**
   - 參數: `string message`
   - 說明: 系統訊息通知

---

## 專案結構

```
ChatRoom/
├── Program.cs                      // 應用程式入口
├── ChatRoom.csproj                 // 專案檔
├── Hubs/
│   └── ChatHub.cs                  // SignalR Hub
├── Models/
│   ├── UserInfo.cs                 // 用戶模型
│   └── ChatMessage.cs              // 訊息模型
├── Services/
│   └── UserConnectionService.cs    // 用戶連線管理服務
└── wwwroot/
    ├── index.html                  // 主頁面
    ├── css/
    │   └── styles.css              // 樣式表
    └── js/
        └── chat.js                 // 聊天邏輯
```

---

## 開發步驟

### Phase 1: 專案初始化
1. 建立 ASP.NET Core Web 專案
2. 安裝 SignalR NuGet 套件
3. 設定專案結構

### Phase 2: 後端開發
1. 建立 Models
2. 建立 UserConnectionService
3. 建立 ChatHub
4. 設定 SignalR 中介軟體
5. 設定 CORS (如需要)

### Phase 3: 前端開發
1. 建立 HTML 結構
2. 建立 CSS 樣式
3. 建立 JavaScript SignalR 客戶端
4. 實作連線功能
5. 實作訊息收發功能
6. 實作用戶列表顯示

### Phase 4: 測試與優化
1. 功能測試
2. 多用戶同時連線測試
3. 斷線重連測試
4. UI/UX 優化
5. 效能優化

---

## 技術考量

### 連線管理
- 使用記憶體內快取管理在線用戶
- 使用 ConcurrentDictionary 確保執行緒安全
- 處理異常斷線情況

### 訊息傳遞
- 廣播訊息給所有連線用戶
- 避免回傳給發送者重複訊息
- 訊息時間戳使用伺服器時間

### 錯誤處理
- 連線失敗重試機制
- 網路中斷自動重連
- 例外狀況記錄

### 安全性考量
- 暱稱長度限制 (最大 20 字元)
- 訊息長度限制 (最大 500 字元)
- 防止 XSS 攻擊（訊息內容編碼）
- 連線頻率限制

---

## 測試計畫

### 功能測試
- [ ] 用戶可以成功連線
- [ ] 用戶可以發送和接收訊息
- [ ] 用戶列表正確顯示
- [ ] 用戶離開時正確更新
- [ ] 系統訊息正確顯示

### 整合測試
- [ ] 多用戶同時連線
- [ ] 多用戶同時發送訊息
- [ ] 用戶加入/離開通知正確

### 壓力測試
- [ ] 10 個用戶同時在線
- [ ] 快速連續發送訊息
- [ ] 長時間連線穩定性

---

## 未來擴充功能

### 優先級 P1
- 私訊功能
- 訊息歷史記錄
- 表情符號支援

### 優先級 P2
- 用戶頭像
- 檔案分享
- 聊天室房間功能
- 管理員權限

### 優先級 P3
- 訊息搜尋
- 多語言支援
- 深色模式
- 訊息已讀狀態

---

## 部署建議

### 開發環境
- Visual Studio 2022 或 Visual Studio Code
- .NET 8.0 SDK
- 現代瀏覽器 (Chrome, Firefox, Edge)

### 生產環境
- IIS 或 Kestrel 伺服器
- HTTPS 憑證
- 反向代理 (Nginx/IIS)
- WebSocket 支援

### 環境變數
- ASPNETCORE_ENVIRONMENT
- ASPNETCORE_URLS
- ConnectionStrings (未來如需資料庫)

---

## 維護與支援

### 文件
- API 文件
- 部署指南
- 使用者手冊

### 版本管理
- 使用語意化版本 (Semantic Versioning)
- 維護 CHANGELOG.md
- Git 分支策略

---

## 附錄

### 參考資源
- [SignalR 官方文件](https://learn.microsoft.com/zh-tw/aspnet/core/signalr/introduction)
- [ASP.NET Core 官方文件](https://learn.microsoft.com/zh-tw/aspnet/core/)
- [.NET 8.0 新功能](https://learn.microsoft.com/zh-tw/dotnet/core/whats-new/dotnet-8)

### 版本記錄
- v1.0.0 - 初始版本（本規格）

---

**文件建立日期**: 2025-12-12  
**最後更新日期**: 2025-12-12  
**文件版本**: 1.0.0
