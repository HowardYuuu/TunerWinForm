# 即時聊天室開發規格文件

## 1. 專案概述

### 1.1 專案名稱
即時聊天室應用程式 (Real-time Chat Room Application)

### 1.2 專案目標
開發一個基於 C# .NET 8.0 與 SignalR 的即時聊天室應用程式，提供使用者即時通訊功能。

### 1.3 技術棧
- **開發語言**: C# 
- **框架版本**: .NET 8.0
- **UI框架**: Windows Forms (WinForms)
- **即時通訊**: ASP.NET Core SignalR
- **目標平台**: Windows (net8.0-windows)

---

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 使用者連線功能
- **功能描述**: 使用者可以輸入名稱並連線進入聊天室
- **輸入要求**:
  - 使用者名稱（必填，長度限制：2-20 個字元）
  - 伺服器位址（預設：localhost）
- **驗證規則**:
  - 名稱不可為空
  - 名稱不可重複
  - 名稱不可包含特殊字元（只允許中文、英文、數字）
- **成功條件**: 
  - 顯示「連線成功」訊息
  - 啟用聊天功能介面
  - 使用者出現在線上列表

#### 2.1.2 即時建立連線事件
- **功能描述**: 即時偵測並通知新使用者加入聊天室
- **事件觸發**:
  - 當有新使用者連線時
  - 當使用者斷線時
- **通知內容**:
  - 系統訊息：「[使用者名稱] 已加入聊天室」
  - 系統訊息：「[使用者名稱] 已離開聊天室」
  - 更新線上人數顯示
- **顯示方式**:
  - 在聊天訊息區以不同顏色顯示系統訊息
  - 時間戳記格式：HH:mm:ss

#### 2.1.3 顯示上線狀態
- **功能描述**: 即時顯示所有線上使用者清單
- **顯示資訊**:
  - 使用者名稱
  - 連線時間
  - 線上狀態指示器（綠點）
- **更新時機**:
  - 有新使用者加入
  - 有使用者離開
  - 重新連線時
- **排序規則**:
  - 依加入時間排序（最新在上）
- **介面位置**:
  - 聊天視窗右側獨立面板
  - 顯示總線上人數

#### 2.1.4 關閉連線功能
- **功能描述**: 使用者可以主動斷開連線並退出聊天室
- **觸發方式**:
  - 點擊「中斷連線」按鈕
  - 關閉應用程式視窗
- **執行動作**:
  - 通知伺服器使用者離開
  - 清除本地連線狀態
  - 禁用聊天功能介面
  - 啟用連線介面
- **異常處理**:
  - 網路中斷時自動清理本地狀態
  - 顯示中斷原因訊息

### 2.2 次要功能

#### 2.2.1 訊息發送功能
- 使用者可以在聊天室中發送文字訊息
- 支援 Enter 鍵快速發送
- 訊息長度限制：1-500 字元
- 防止空訊息發送

#### 2.2.2 訊息接收顯示
- 即時接收並顯示其他使用者訊息
- 訊息格式：[時間] 使用者名稱：訊息內容
- 自己的訊息與他人訊息使用不同顏色區分
- 自動捲動至最新訊息

#### 2.2.3 歷史訊息
- 顯示本次連線的所有歷史訊息
- 訊息數量限制：最多保留 200 條
- 超過限制時自動刪除最舊訊息

---

## 3. 系統架構

### 3.1 整體架構

```
┌─────────────────────────────────────────┐
│         WinForms Client Application     │
│  ┌───────────────────────────────────┐  │
│  │   Chat UI (Form)                  │  │
│  │  - 連線面板                        │  │
│  │  - 聊天訊息顯示區                  │  │
│  │  - 訊息輸入區                      │  │
│  │  - 線上使用者列表                  │  │
│  └───────────────┬───────────────────┘  │
│                  │                       │
│  ┌───────────────▼───────────────────┐  │
│  │   SignalR Client Service          │  │
│  │  - HubConnection 管理              │  │
│  │  - 事件處理器                      │  │
│  │  - 重連邏輯                        │  │
│  └───────────────┬───────────────────┘  │
└──────────────────┼───────────────────────┘
                   │ SignalR Protocol
                   │ (WebSocket/SSE/Long Polling)
┌──────────────────▼───────────────────────┐
│    ASP.NET Core SignalR Server           │
│  ┌───────────────────────────────────┐   │
│  │   ChatHub (SignalR Hub)           │   │
│  │  - OnConnectedAsync()             │   │
│  │  - OnDisconnectedAsync()          │   │
│  │  - SendMessage()                  │   │
│  │  - JoinChat()                     │   │
│  └───────────────┬───────────────────┘   │
│                  │                        │
│  ┌───────────────▼───────────────────┐   │
│  │   Connection Manager              │   │
│  │  - 使用者連線追蹤                  │   │
│  │  - 線上狀態管理                    │   │
│  │  - 使用者名稱映射                  │   │
│  └───────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

### 3.2 專案結構

```
ChatRoomApp/
├── ChatRoomServer/                    # SignalR 伺服器專案
│   ├── Program.cs                     # 伺服器進入點
│   ├── Hubs/
│   │   └── ChatHub.cs                 # SignalR Hub 實作
│   ├── Services/
│   │   └── ConnectionManager.cs      # 連線管理服務
│   ├── Models/
│   │   ├── ChatMessage.cs            # 訊息模型
│   │   └── UserConnection.cs         # 使用者連線模型
│   └── ChatRoomServer.csproj          # 專案檔
│
└── ChatRoomClient/                    # WinForms 用戶端專案
    ├── Program.cs                     # 用戶端進入點
    ├── Forms/
    │   ├── MainForm.cs                # 主聊天視窗
    │   └── MainForm.Designer.cs       # 視窗設計器檔案
    ├── Services/
    │   └── ChatService.cs             # SignalR 客戶端服務
    ├── Models/
    │   ├── ChatMessage.cs             # 訊息模型（與伺服器共用）
    │   └── User.cs                    # 使用者模型
    └── ChatRoomClient.csproj          # 專案檔
```

---

## 4. 技術規格

### 4.1 SignalR Hub 設計

#### 4.1.1 ChatHub 類別

```csharp
public class ChatHub : Hub
{
    // 伺服器端方法
    Task OnConnectedAsync()              // 連線建立時呼叫
    Task OnDisconnectedAsync()           // 連線中斷時呼叫
    Task JoinChat(string userName)       // 使用者加入聊天室
    Task SendMessage(string message)     // 發送訊息
    Task LeaveChat()                     // 使用者離開聊天室
}
```

#### 4.1.2 客戶端方法（由伺服器呼叫）

```csharp
// 客戶端需要實作的接收方法
"ReceiveMessage"          // 接收聊天訊息
"UserJoined"              // 通知使用者加入
"UserLeft"                // 通知使用者離開
"UpdateUserList"          // 更新線上使用者列表
"ReceiveSystemMessage"    // 接收系統訊息
```

### 4.2 資料模型

#### 4.2.1 ChatMessage（聊天訊息）

```csharp
public class ChatMessage
{
    public string UserName { get; set; }      // 使用者名稱
    public string Message { get; set; }       // 訊息內容
    public DateTime Timestamp { get; set; }   // 時間戳記
    public MessageType Type { get; set; }     // 訊息類型（一般/系統）
}

public enum MessageType
{
    UserMessage,     // 使用者訊息
    SystemMessage    // 系統訊息
}
```

#### 4.2.2 UserConnection（使用者連線）

```csharp
public class UserConnection
{
    public string ConnectionId { get; set; }  // SignalR 連線 ID
    public string UserName { get; set; }      // 使用者名稱
    public DateTime ConnectedAt { get; set; } // 連線時間
}
```

#### 4.2.3 User（使用者資訊）

```csharp
public class User
{
    public string UserName { get; set; }      // 使用者名稱
    public DateTime JoinedAt { get; set; }    // 加入時間
    public bool IsOnline { get; set; }        // 線上狀態
}
```

### 4.3 連線管理

#### 4.3.1 ConnectionManager 服務

```csharp
public class ConnectionManager
{
    // 儲存連線映射
    private Dictionary<string, UserConnection> _connections;
    
    // 方法
    void AddConnection(string connectionId, string userName)
    void RemoveConnection(string connectionId)
    UserConnection GetConnection(string connectionId)
    List<UserConnection> GetAllConnections()
    bool IsUserNameTaken(string userName)
    int GetOnlineUserCount()
}
```

### 4.4 通訊協定

#### 4.4.1 傳輸方式優先順序
1. WebSocket（首選）
2. Server-Sent Events (SSE)
3. Long Polling（後備）

#### 4.4.2 訊息格式
- 使用 JSON 格式序列化
- 自動處理編碼問題（UTF-8）

### 4.5 錯誤處理

#### 4.5.1 連線錯誤
- 連線失敗：顯示錯誤訊息，允許重試
- 連線中斷：自動重連（最多 5 次，間隔 5 秒）
- 重連失敗：通知使用者手動重連

#### 4.5.2 訊息錯誤
- 發送失敗：提示使用者重新發送
- 接收錯誤：記錄錯誤但不中斷連線

---

## 5. 使用者介面設計

### 5.1 主視窗佈局

```
┌─────────────────────────────────────────────────────────┐
│  即時聊天室                                      [-][□][X]│
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐│
│  │ 連線設定面板 (連線前顯示)                           ││
│  │  使用者名稱: [________________]                     ││
│  │  伺服器位址: [localhost:5000  ]                     ││
│  │              [   連    線   ]                        ││
│  └─────────────────────────────────────────────────────┘│
│                                                           │
│  ┌───────────────────────────────┬───────────────────┐  │
│  │ 聊天訊息顯示區                 │  線上使用者       │  │
│  │                               │  ═══════════════  │  │
│  │ [系統] 歡迎來到聊天室          │  線上人數: 3      │  │
│  │ [10:30:15] 小明: 大家好！      │                   │  │
│  │ [10:30:20] 小華: 你好！        │  ● 小明           │  │
│  │ [10:30:25] 我: 早安！          │  ● 小華           │  │
│  │                               │  ● 我             │  │
│  │                               │                   │  │
│  │                               │                   │  │
│  │                               │                   │  │
│  │                               │                   │  │
│  └───────────────────────────────┴───────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐│
│  │ 訊息輸入區                                           ││
│  │ [_____________________________________________]      ││
│  │                          [ 發送 ] [ 中斷連線 ]      ││
│  └─────────────────────────────────────────────────────┘│
│  狀態列: 已連線 | 伺服器: localhost:5000                 │
└─────────────────────────────────────────────────────────┘
```

### 5.2 控制項規格

#### 5.2.1 連線設定面板
- **使用者名稱輸入框** (TextBox)
  - Name: txtUserName
  - MaxLength: 20
  - PlaceholderText: "請輸入您的名稱"
  
- **伺服器位址輸入框** (TextBox)
  - Name: txtServerUrl
  - DefaultValue: "http://localhost:5000"
  
- **連線按鈕** (Button)
  - Name: btnConnect
  - Text: "連線"
  - Enabled: 僅在輸入名稱後啟用

#### 5.2.2 聊天訊息顯示區
- **訊息顯示框** (RichTextBox)
  - Name: rtbMessages
  - ReadOnly: true
  - ScrollBars: Vertical
  - WordWrap: true
  - Font: Microsoft JhengHei, 10pt

#### 5.2.3 線上使用者面板
- **人數標籤** (Label)
  - Name: lblOnlineCount
  - Text: "線上人數: 0"
  
- **使用者列表** (ListBox)
  - Name: lstUsers
  - Font: Microsoft JhengHei, 10pt

#### 5.2.4 訊息輸入區
- **訊息輸入框** (TextBox)
  - Name: txtMessage
  - MaxLength: 500
  - Multiline: false
  - PlaceholderText: "輸入訊息..."
  - Enabled: 僅在連線後啟用
  
- **發送按鈕** (Button)
  - Name: btnSend
  - Text: "發送"
  - Enabled: 僅在連線且有輸入內容時啟用
  
- **中斷連線按鈕** (Button)
  - Name: btnDisconnect
  - Text: "中斷連線"
  - Enabled: 僅在連線後啟用

#### 5.2.5 狀態列
- **狀態標籤** (StatusStrip)
  - Name: statusStrip
  - Items:
    - lblConnectionStatus: 顯示連線狀態
    - lblServerInfo: 顯示伺服器資訊

### 5.3 色彩配置

- **使用者訊息**: 黑色 (#000000)
- **自己的訊息**: 藍色 (#0066CC)
- **系統訊息**: 灰色 (#666666)
- **錯誤訊息**: 紅色 (#CC0000)
- **線上指示器**: 綠色 (#00CC00)

### 5.4 字型設定

- **主要字型**: Microsoft JhengHei (微軟正黑體)
- **訊息顯示**: 10pt
- **使用者列表**: 10pt
- **按鈕文字**: 9pt

---

## 6. API 規格

### 6.1 伺服器端 Hub 方法

#### 6.1.1 JoinChat
**描述**: 使用者加入聊天室  
**參數**:
- `userName` (string): 使用者名稱  

**返回**: Task  
**異常**:
- `HubException`: 當使用者名稱已被使用時  

**行為**:
1. 驗證使用者名稱是否可用
2. 將連線 ID 與使用者名稱關聯
3. 廣播使用者加入事件到所有客戶端
4. 發送最新的使用者列表給所有客戶端

#### 6.1.2 SendMessage
**描述**: 發送聊天訊息  
**參數**:
- `message` (string): 訊息內容  

**返回**: Task  
**異常**:
- `HubException`: 當使用者未加入聊天室時  

**行為**:
1. 取得發送者的使用者名稱
2. 建立訊息物件（包含時間戳記）
3. 廣播訊息到所有客戶端

#### 6.1.3 LeaveChat
**描述**: 使用者離開聊天室  
**參數**: 無  
**返回**: Task  

**行為**:
1. 移除連線記錄
2. 廣播使用者離開事件到所有客戶端
3. 發送最新的使用者列表給所有客戶端

### 6.2 客戶端回呼方法

#### 6.2.1 ReceiveMessage
**描述**: 接收聊天訊息  
**參數**:
- `userName` (string): 發送者名稱
- `message` (string): 訊息內容
- `timestamp` (DateTime): 時間戳記

#### 6.2.2 UserJoined
**描述**: 使用者加入通知  
**參數**:
- `userName` (string): 加入的使用者名稱

#### 6.2.3 UserLeft
**描述**: 使用者離開通知  
**參數**:
- `userName` (string): 離開的使用者名稱

#### 6.2.4 UpdateUserList
**描述**: 更新線上使用者列表  
**參數**:
- `users` (List<string>): 線上使用者名稱列表

#### 6.2.5 ReceiveSystemMessage
**描述**: 接收系統訊息  
**參數**:
- `message` (string): 系統訊息內容
- `timestamp` (DateTime): 時間戳記

---

## 7. 開發階段

### 7.1 第一階段：伺服器開發
**時程**: 1-2 天  
**任務**:
- [ ] 建立 ASP.NET Core SignalR 專案
- [ ] 實作 ChatHub 類別
- [ ] 實作 ConnectionManager 服務
- [ ] 實作資料模型（ChatMessage, UserConnection）
- [ ] 設定 CORS 政策
- [ ] 實作錯誤處理機制
- [ ] 伺服器單元測試

**交付物**:
- ChatRoomServer 專案
- 可執行的 SignalR 伺服器

### 7.2 第二階段：客戶端開發
**時程**: 2-3 天  
**任務**:
- [ ] 建立 WinForms 專案
- [ ] 設計主視窗 UI（MainForm）
- [ ] 實作 ChatService（SignalR 客戶端）
- [ ] 實作連線功能
- [ ] 實作訊息發送與接收
- [ ] 實作使用者列表更新
- [ ] 實作中斷連線功能
- [ ] UI 事件處理

**交付物**:
- ChatRoomClient 專案
- 完整的聊天室客戶端

### 7.3 第三階段：整合測試
**時程**: 1 天  
**任務**:
- [ ] 伺服器與客戶端整合測試
- [ ] 多使用者同時連線測試
- [ ] 連線中斷與重連測試
- [ ] 訊息傳遞測試
- [ ] 使用者列表更新測試
- [ ] 效能測試（10+ 使用者）

**交付物**:
- 測試報告
- 已修正的錯誤

### 7.4 第四階段：文件與部署
**時程**: 1 天  
**任務**:
- [ ] 撰寫使用者手冊
- [ ] 撰寫部署指南
- [ ] 準備發行版本
- [ ] 建立安裝程式

**交付物**:
- 使用者手冊
- 部署指南
- 發行版本

---

## 8. 測試計畫

### 8.1 單元測試

#### 8.1.1 伺服器端測試
- **ConnectionManager 測試**
  - 測試新增連線
  - 測試移除連線
  - 測試取得連線資訊
  - 測試使用者名稱重複驗證
  
- **ChatHub 測試**
  - 測試使用者加入
  - 測試訊息發送
  - 測試使用者離開
  - 測試異常處理

#### 8.1.2 客戶端測試
- **ChatService 測試**
  - 測試連線建立
  - 測試訊息發送
  - 測試訊息接收
  - 測試連線中斷

### 8.2 整合測試

#### 8.2.1 功能測試
- **連線測試**
  - ✓ 正常連線
  - ✓ 重複名稱連線（應失敗）
  - ✓ 空名稱連線（應失敗）
  - ✓ 伺服器關閉時連線（應失敗）
  
- **訊息測試**
  - ✓ 發送普通訊息
  - ✓ 發送空訊息（應失敗）
  - ✓ 發送超長訊息（應被截斷）
  - ✓ 接收他人訊息
  
- **使用者列表測試**
  - ✓ 使用者加入時列表更新
  - ✓ 使用者離開時列表更新
  - ✓ 重新連線後列表正確
  
- **中斷連線測試**
  - ✓ 主動中斷連線
  - ✓ 網路中斷時的處理
  - ✓ 伺服器關閉時的處理

#### 8.2.2 效能測試
- **負載測試**
  - 同時 10 個使用者連線
  - 同時 50 個使用者連線
  - 每秒 100 則訊息傳輸
  
- **穩定性測試**
  - 長時間連線（24 小時）
  - 頻繁連線/斷線

### 8.3 使用者驗收測試

#### 8.3.1 測試情境
1. **新使用者加入**
   - 開啟應用程式
   - 輸入名稱
   - 點擊連線
   - 驗證：能看到歡迎訊息和其他使用者
   
2. **發送訊息**
   - 在已連線狀態
   - 輸入訊息
   - 點擊發送或按 Enter
   - 驗證：訊息出現在聊天區，其他使用者也能看到
   
3. **離開聊天室**
   - 在已連線狀態
   - 點擊中斷連線
   - 驗證：連線被中斷，UI 恢復到未連線狀態

---

## 9. 安全性考量

### 9.1 輸入驗證
- 使用者名稱長度限制（2-20 字元）
- 訊息長度限制（1-500 字元）
- 特殊字元過濾
- XSS 攻擊防護（HTML 編碼）

### 9.2 連線安全
- 支援 HTTPS/WSS 連線
- 連線逾時設定（30 秒）
- 防止暴力連線攻擊

### 9.3 訊息安全
- 訊息內容驗證
- 防止訊息洪水攻擊（限制每使用者每秒最多 5 則訊息）

---

## 10. 效能要求

### 10.1 回應時間
- 連線建立：< 1 秒
- 訊息傳遞延遲：< 100 毫秒
- 使用者列表更新：< 500 毫秒

### 10.2 並發能力
- 同時支援至少 100 個使用者連線
- 每秒處理至少 1000 則訊息

### 10.3 資源使用
- 客戶端記憶體使用：< 100 MB
- 伺服器記憶體使用（100 使用者）：< 500 MB

---

## 11. 部署需求

### 11.1 伺服器環境
- **作業系統**: Windows Server 2019+ / Linux
- **執行環境**: .NET 8.0 Runtime
- **網路**: 開放 HTTP/HTTPS 埠（預設 5000/5001）

### 11.2 客戶端環境
- **作業系統**: Windows 10/11
- **執行環境**: .NET 8.0 Desktop Runtime
- **網路**: 能連線到伺服器的網路環境

### 11.3 開發環境
- **IDE**: Visual Studio 2022 (17.8+)
- **SDK**: .NET 8.0 SDK
- **版本控制**: Git

---

## 12. 依賴套件

### 12.1 伺服器端套件
```xml
<PackageReference Include="Microsoft.AspNetCore.SignalR" Version="1.1.0" />
```

### 12.2 客戶端套件
```xml
<PackageReference Include="Microsoft.AspNetCore.SignalR.Client" Version="8.0.0" />
```

---

## 13. 未來擴充功能

### 13.1 進階功能（未來版本）
- [ ] 私人訊息功能
- [ ] 聊天室多房間功能
- [ ] 檔案傳輸功能
- [ ] 訊息歷史持久化（資料庫）
- [ ] 使用者認證與授權
- [ ] 表情符號支援
- [ ] 訊息搜尋功能
- [ ] 使用者封鎖功能
- [ ] 管理員功能（踢出使用者）

### 13.2 技術改進
- [ ] 使用 Redis 作為後端儲存（支援多伺服器）
- [ ] 實作訊息加密
- [ ] 支援語音/視訊通話
- [ ] 移動平台支援（Xamarin/MAUI）
- [ ] Web 版本客戶端

---

## 14. 附錄

### 14.1 參考資料
- [ASP.NET Core SignalR 官方文件](https://docs.microsoft.com/aspnet/core/signalr)
- [SignalR JavaScript 客戶端](https://docs.microsoft.com/aspnet/core/signalr/javascript-client)
- [Windows Forms 官方文件](https://docs.microsoft.com/dotnet/desktop/winforms)

### 14.2 版本歷史
- **v1.0** (2024-12-12): 初始版本規格

### 14.3 聯絡資訊
- **專案負責人**: [待填寫]
- **開發團隊**: [待填寫]
- **技術支援**: [待填寫]

---

## 15. 批准簽核

| 角色 | 姓名 | 簽名 | 日期 |
|------|------|------|------|
| 專案經理 | | | |
| 技術主管 | | | |
| 客戶代表 | | | |

---

**文件版本**: 1.0  
**最後更新**: 2024-12-12  
**狀態**: 待審核
